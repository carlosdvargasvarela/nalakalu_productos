<!DOCTYPE html>
<html lang="es">
  <head>
    <title>Inventario — Nalakalú Productos</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/css/tom-select.bootstrap5.min.css">
    <style>
      .inv-sidebar {
        width: 220px;
        min-width: 220px;
        background: #fff;
        border-right: 1px solid #dee2e6;
        flex-shrink: 0;
      }
      .inv-sidebar .sidebar-section {
        font-size: 0.67rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #adb5bd;
        padding: 1rem 1.1rem 0.25rem;
      }
      .inv-sidebar .sidebar-link {
        display: flex;
        align-items: center;
        padding: 0.42rem 0.75rem;
        margin: 0.05rem 0.5rem;
        border-radius: 0.375rem;
        color: #495057;
        text-decoration: none;
        font-size: 0.875rem;
        line-height: 1.4;
        transition: background-color 0.1s;
        gap: 0.55rem;
      }
      .inv-sidebar .sidebar-link:hover {
        background: #f1f3f5;
        color: #212529;
      }
      .inv-sidebar .sidebar-link.active {
        background: #e7f1ff;
        color: #0d6efd;
        font-weight: 600;
      }
      .inv-sidebar .sidebar-link.active i {
        color: #0d6efd;
      }
      .inv-sidebar .sidebar-link i {
        font-size: 0.95rem;
        width: 1rem;
        text-align: center;
        color: #6c757d;
        flex-shrink: 0;
      }
      .inv-sidebar .sidebar-divider {
        height: 1px;
        background: #f1f3f5;
        margin: 0.5rem 1rem;
      }
    </style>
  </head>
  <body class="bg-light d-flex flex-column min-vh-100">
    <%= render "layouts/navbar" %>
    <div class="d-flex flex-grow-1">
      <%= render "inventory/shared/sidebar" %>
      <main class="flex-grow-1 px-4 py-4" style="min-width: 0;">
        <div id="flash_container">
          <%= render "layouts/flash_messages" %>
        </div>
        <%= yield %>
      </main>
    </div>
    <%= turbo_frame_tag "remote_modal" %>
    <%= render "layouts/footer" %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Sortable/1.15.2/Sortable.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/js/tom-select.complete.min.js"></script>
  </body>
</html>
