<%# 1. Agregar fila al panel izquierdo (dentro del grupo del tipo) %>
<%= turbo_stream.prepend "variant-group-#{@variant.variant_type_id}" do %>
  <%= render "variants/variant_list_item", variant: @variant, selected: true %>
<% end %>
<%# 2. Cargar el show panel en el workspace %>
<%= turbo_stream.update "variant_workspace" do %>
  <%= render "variants/show_panel",
      variant: @variant,
      compatible_products: compatible_products_for(@variant) %>
<% end %>
<%# 3. Actualizar tabla en panel de tipo de variante si está abierto %>
<%= turbo_stream.prepend "variants-table-body" do %>
  <%= render "variant_types/variant_row", variant: @variant %>
<% end %>
<%# 4. Señal para cerrar modal si aplica %>
<%= turbo_stream.replace "variant-modal-signal" do %>
  <div id="variant-modal-signal" data-variant-modal-target="signal"></div>
<% end %>
